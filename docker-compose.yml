version: '3.8'
services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./knowledge:/app/knowledge
      - ./processor.log:/app/processor.log
